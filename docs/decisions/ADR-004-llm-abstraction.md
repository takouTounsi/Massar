# ADR-004 LLM Abstraction

Status: accepted

LLMs are only used for language generation and explanation. `MockLLMProvider` is the default and requires no API key.

An OpenAI-compatible provider is available through environment variables for later use.
