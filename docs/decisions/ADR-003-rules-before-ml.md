# ADR-003 Rules Before ML

Status: accepted

The first version uses rule-based decisions with explicit evidence and rule IDs. ML interfaces are present, but rule models remain the active providers.

This gives explainability, deterministic tests and safer hackathon behavior.
